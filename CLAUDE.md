@AGENTS.md

# 프로젝트 규칙 (CLAUDE.md)

## 프로젝트 개요
마케팅 콘텐츠의 법무·컴플라이언스 리스크를 사전 검토하고, 문제 표현에 대한 수정안을 제안하는 AI 검수 어시스턴트. 자세한 배경·목표·범위는 [PRD.md](PRD.md) 참고.

## 참고 문서
- **PRD.md**: 기능·범위·목표에 대한 결정 기준. 기능 추가/변경 시 PRD.md와 먼저 맞춰보고, 충돌하면 코드를 바꾸기 전에 사용자에게 확인한다.

## 기술 스택
- Next.js (App Router) + TypeScript + Tailwind CSS
- 배포 대상: Vercel
- 다른 프레임워크/스택 제안 금지 (PRD 8번에서 Next.js로 고정)

## 개발 명령어
- `npm run dev` — 개발 서버 실행
- `npm run build` — 프로덕션 빌드
- `npm run lint` — 린트 검사

## 폴더 구조
- `app/` — 화면(페이지) 및 API 라우트
- `app/layout.tsx` — 공통 레이아웃
- `app/page.tsx` — 첫 화면
- `app/globals.css` — 전역 스타일 (Tailwind)
- `public/` — 정적 파일

## 이 앱의 AI 기능이 지켜야 할 규칙 (PRD 5번 기준)
- 리스크로 지적할 때는 반드시 판단 근거(관련 규정 또는 사유)를 함께 제시한다.
- 근거가 불명확한 표현은 "위반"으로 단정하지 않고 "확인 필요"로 별도 표시한다.
- 대체 표현(수정안)은 원문의 마케팅 의도와 핵심 의미를 벗어나지 않는 범위에서만 제안하고, 원문에 없는 사실·효능을 새로 추가하지 않는다.
- 수정안은 항상 최소 1개 이상 제시한다.

## 보안·개인정보 규칙 (PRD 7번 기준)
- API 키 등 민감 정보는 서버 측 환경변수(`.env.local`, Vercel 환경변수)로만 관리한다.
- API 키를 클라이언트 코드, 커밋, 로그에 노출하지 않는다.
- `.env*` 파일은 git에 커밋하지 않는다 (`.gitignore` 확인).
- 입력 콘텐츠는 사내한정(대외비) 자료로 취급하고, 개인정보(실명·연락처 등)가 포함되지 않도록 안내한다.

## 이번 v1에서 만들지 않는 것 (PRD 6번 기준)
- 콘텐츠를 실제로 게시·배포하는 기능
- 이미지·캐러셀·영상 콘텐츠 분석
- 한국어 외 다국어(영어·일본어·스페인어 등) 지원
- 사용자 계정·권한 관리, 다중 테넌트 기능

이 항목들은 사용자가 명시적으로 요청하기 전까지 구현하지 않는다.

## 작업 방식 규칙
- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 my-app 폴더 안에만 만든다.
- 기술 스택은 PRD.md에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.
