# 개발 계획 및 로드맵 (PLAN)

- 작성일: 2026-09-17
- 기준: 현재 배포된 프로덕션(Vercel, GitHub `main` 최신 커밋)

이 문서는 "지금 무엇이 끝났고, 다음에 무엇을 해야 하는지"를 추적하기 위한 문서다. 기능 범위·목표에 대한 결정 기준은 [PRD.md](PRD.md), 개발 규칙은 [CLAUDE.md](CLAUDE.md)를 따른다.

## 1) 현재까지 완료된 것 (v1, 배포 완료)

- 텍스트 콘텐츠 리스크 검수: 마케팅 표현 / 의료기기 광고 / 콘텐츠 권리·동의 / 환자 정보 / 법정 표기 사항 5개 영역, SAFE·CONDITIONAL·HIGH RISK 3단계 등급
- 판단 근거 다각화: 법령 원문 + 사내 판단 기준 + 식약처 실제 광고 심의사례(181건) + 제품별 허가 사용목적 대조
- 대체 표현 제안 + 수정본 전체 자동 생성, 한국어/영어 중 원하는 언어로 출력
- 검수 결과 재현성 확보 (`temperature: 0`, 등급 고정 규칙 등으로 동일 입력 시 동일 결과)
- 로그인/가입: 이메일·비밀번호, `@jeisys.com` 도메인 제한(Before User Created 훅) + 6자리 이메일 인증번호(SendGrid 커스텀 SMTP) 2중 확인
- 역할 구분: `admin` / `user`, 사례(판단 근거) 추가·수정·삭제는 로그인한 모두 가능 + 모든 변경 이력 기록
- GitHub 저장소(Public) + Vercel 배포, GitHub push 시 자동 배포 연결 완료
- 소개 발표자료(PPTX) 제작

## 2) 알려진 이슈 (우선 확인 필요)

### 🔴 관리자 역할이 로그인 세션(JWT)에 반영되지 않음
- **증상**: Supabase Auth의 Custom Access Token Hook(`custom_access_token_hook`)을 대시보드/Management API에서 활성화했고, 함수 자체를 직접 호출하면 `app_metadata.role`이 정상적으로 채워지는 것을 확인했다. 그런데 실제 로그인(`signInWithPassword`)으로 발급되는 JWT에는 여전히 `role`이 포함되지 않는다. (2026-09-16 최초 발견, 2026-09-17 재확인 — 하루가 지나도 동일)
- **영향**: `middleware.ts`가 role을 못 읽으면 모든 사용자를 `user`로 취급한다 — **권한이 과도하게 부여되는 보안 문제는 아니지만(fail-safe)**, 실제 `admin@jeisys.com` 계정도 `/admin/history`(변경 이력 조회) 화면에 지금 접근하지 못하고 있을 가능성이 높다.
- **다음에 시도해볼 것**:
  - Supabase 대시보드(Authentication → Hooks)에서 Custom Access Token 훅이 화면상으로도 "연결됨"으로 보이는지 직접 확인
  - Supabase 지원팀 문의 또는 커뮤니티 사례 확인 (Management API로 활성화한 설정이 실제 Auth 서버에 반영되지 않는 알려진 이슈가 있는지)
  - 임시 우회책: 미들웨어에서 role을 JWT 대신 `profiles` 테이블을 직접 조회해서 판단하도록 변경 (매 요청 DB 조회가 추가되지만 확실히 동작)

## 3) 향후 확장 (PRD 6번 "향후 확장" 항목, 아직 미착수)

- 이미지·캐러셀·영상 콘텐츠에 대한 리스크 탐지
- 일본어·스페인어 등 한국어·영어 외 추가 언어 지원
- 다중 테넌트(여러 회사가 함께 쓰는 구조) 기능
- 담당자별 검수 이력 저장·조회 (누가 언제 무엇을 검수했는지)

## 4) 운영 후속 조치 (기능 개발은 아니지만 처리 필요)

- **GitHub 토큰 재발급 권장**: 개발 중 실수로 로컬 `.git/config`에 토큰이 평문 노출된 적이 있어(원격 저장소에는 노출 안 됨) 안전을 위해 `GITHUB_TOKEN` 재발급 권장. 아직 미실행.
- **식약처 심의사례 추가 반영 검토**: 공공데이터 181건 중 형식 검증을 통과하지 못해 제외된 나머지 사례들의 재정제·추가 삽입 여부 결정 필요.
- **Supabase 이메일 발송 한도 점검**: 커스텀 SMTP(SendGrid) 연결 후 시간당 발송 한도를 100통으로 상향했음. 사용자가 늘어나면 SendGrid 발신 한도(무료 티어 기준)도 함께 점검 필요.
- **테스트용 계정 정리**: 개발/테스트 중 생성된 `junho.kim@jeisys.com`, `DigitalMarketingTeam@jeisys.com` 등 테스트 계정을 실제 운영 전환 시 정리할지 결정 필요.
